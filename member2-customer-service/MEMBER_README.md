# Member 2 — Customer Service

Own customer registration, unique-email checking and profile CRUD. Main endpoints: `GET /customers`, `GET /customers/{id}`, `POST /customers`, `PUT /customers/{id}`, `DELETE /customers/{id}`. Your container listens on `8082` and uses `customerdb`. Push this complete folder to your member branch and explain the record model, repository, controller, validation and Dockerfile.
